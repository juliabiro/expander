# expander
a commandline go tool that expands abbreviations

## use

Put the list of abbreviations into a file and set its path as the EXPANDER_CONF envvar. 

Build with `make build`. 

Run as `main <space separated list of abbreviations >`

